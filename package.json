{
  "name": "donusum-kapisi",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --filter=web",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "postinstall": "pnpm --filter @donusum-kapisi/db generate"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20"
  }
}
